"""보고서 1·2장의 고정 텍스트. 근거가 필요한 사실·수치는 넣지 않고, 인용 문단은 LLM/코드가 뒤에 붙인다."""

from techeval.schemas import TechRef

BACKGROUND_TEMPLATE = """\
Transformer 기반 LLM은 디코딩 단계에서 앞선 토큰의 Key·Value 벡터를 KV cache로 보관해 재계산을 피한다. \
KV cache의 크기는 층 수·헤드 차원·컨텍스트 길이·동시 요청 수에 비례해 커지므로, \
긴 입력과 많은 동시 세션이 겹치는 추론 환경에서는 가속기 메모리의 용량과 대역폭이 계산보다 먼저 한계에 닿는다(일반 구조 설명).

이 병목을 다루는 접근은 크게 두 갈래로 나뉜다. 하나는 SW 측에서 KV cache 자체를 줄이는 방법(어텐션 구조 재설계, 양자화, 토큰 선별 등)이고, \
다른 하나는 HW 측에서 메모리 계층을 넓히거나 연산 일부를 메모리 쪽으로 옮기는 방법(CXL 메모리 확장, 메모리 근처 연산 등)이다. \
본 보고서는 두 갈래에서 기술을 하나씩 골라 같은 기준으로 평가하고, 관점에 따라 평가가 어떻게 달라지는지를 정리한다.

평가 대상 환경은 **{domain}**으로 고정한다. 이 환경은 컨텍스트 길이와 동시 처리량이 함께 커지는 조건이어서 KV cache 부담이 가장 직접적으로 드러나고, \
SW 축소와 HW 확장이 서로 다른 비용 구조(모델 변경 대 인프라 변경)를 요구한다는 차이가 판정에 반영되기 쉽다.
"""

SELECTION_TEMPLATE = """\
기술 선정은 사람이 고정했으며 에이전트가 선택하지 않았다. 선정 기준은 **대표성**(각 접근을 대표하는 설계인가), \
**대조성**(두 기술이 서로 다른 변경 범위를 전제하는가), **자료 확보 가능성**(원문·공개 구현·계열 자료를 확보할 수 있는가) 세 가지다.

| 접근 | 기술 | 선정 논문 | 발행 | 기술 계열(시장·이해관계자 조사 범위) |
|---|---|---|---|---|
{tech_rows}

{reasons}

비교 후보(TurboQuant·KIVI 등 KV cache 양자화·압축 계열)는 `kv_survey`의 KV cache 관리 기법 분류를 참고해 검토했으며, \
위 세 기준을 함께 충족하는 조합으로 두 기술을 고정했다.
"""

SELECTION_REASONS: dict[str, str] = {
    "mla": (
        "- **{name}**: 대표성 — 어텐션 구조를 재설계해 KV를 저차원 잠재 공간으로 압축하는 SW 접근이다{kv_claim}. "
        "대조성 — 기존 GPU·HBM 인프라에서 운용할 수 있으나 모델 구조와 서빙 최적화가 필요하다. "
        "자료 확보 — 공개 모델·논문·서빙 프레임워크 자료가 있다."
    ),
    "pim_cxl": (
        "- **{name}**: 대표성 — KV cache를 CXL 메모리로 확장하고 토큰 페이지 선택 연산을 메모리 측 가속기로 옮기는 HW 접근이다{kv_claim}. "
        "대조성 — 서버 HW·메모리 계층·서빙 경로 변경을 전제한다. "
        "자료 확보 — 논문 실험과 CXL·PIM/PNM 벤더 발표·표준화·후속 연구가 있다. "
        "논문 시제품과 기술 계열 전체의 상용화 근거는 구분해 기록했다."
    ),
}

CH4_INTRO = (
    "각 절은 두 기술에 같은 기준을 적용한 결과다. 절 말미 요약표의 레벨은 기준별 판정 단계이며 "
    "관점·기준이 다른 레벨끼리 더하거나 묶어 하나의 값으로 만들지 않는다. 근거 단위는 논문·구현(paper)과 기술 계열(family)로 구분해 적었다."
)


def tech_rows(technologies: list[TechRef]) -> str:
    approach = {"sw": "SW — KV cache 축소", "hw": "HW — 메모리 계층 확장"}
    return "\n".join(
        f"| {approach[t.approach]} | {t.name} | {t.paper_title} | {t.paper_date} | {t.family} |" for t in technologies
    )
